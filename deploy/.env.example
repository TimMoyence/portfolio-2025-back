BACK_IMAGE=ghcr.io/<owner>/portfolio-2025-back:latest
FRONT_IMAGE=ghcr.io/<owner>/portfolio-2025-front:latest
API_PORT=3000
WEB_PORT=4000
# --- API ---
PORT=3000
API_PORT=3000
API_PREFIX=api
SWAGGER_PATH=docs
CORS_ORIGIN=https://asilidesign.fr
TYPEORM_SYNCHRONIZE=false
DB_SYNCHRONIZE=false

# --- Database (Postgres) ---
DB_HOST=db
DB_PORT=5432
POSTGRES_DB=pareil
PGDATABASE=lenomdeladatabase
POSTGRES_USER=lenomduuser
POSTGRES_PASSWORD=LeCOde
DB_SSL=false
# Optional DATABASE_URL takes precedence over the individual values above.
DATABASE_URL=

# --- Audit pipeline / queue ---
AUDIT_QUEUE_ENABLED=true
AUDIT_QUEUE_NAME=audit_requests
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
AUDIT_QUEUE_CONCURRENCY=2
AUDIT_QUEUE_ATTEMPTS=3
AUDIT_QUEUE_BACKOFF_MS=2000
AUDIT_JOB_TIMEOUT_MS=180000

# --- Admin database used to auto-create the DB when DATABASE_URL is set ---
DB_ADMIN_DATABASE=postgres
